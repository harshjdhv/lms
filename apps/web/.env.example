# Optional: add these to .env.local for captionless video transcription (direct .mp4/.mp3 URLs).
# Set one of these; TRANSCRIPT_STT_PROVIDER=deepgram or assemblyai picks when both are set.
# DEEPGRAM_API_KEY=
# ASSEMBLYAI_API_KEY=
# Webhook base (defaults to VERCEL_URL in prod): NEXT_PUBLIC_APP_URL=https://your-app.vercel.app
